const NotFound = ({text}) => {
    return (
        <div className="pd-card" style={ {textAlign: 'center'} }>
            <p>
                {text}
            </p>
        </div>
    );
}

export default NotFound;